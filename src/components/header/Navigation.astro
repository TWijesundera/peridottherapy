---
import type { HTMLAttributes } from "astro/types";
import PeridotHeaderLink from "@components/header/PeridotHeaderLink.astro";

type Props = HTMLAttributes<"nav">;

const { class: className, ...props } = Astro.props;
const links: Record<string, string> = {
  Services: "/#services",
  Contact: "/#contact-form",
  About: "/about",
};
---

<div id="nav" class:list={[className]} {...props}>
  {
    Object.keys(links).map((title) => (
      <PeridotHeaderLink
        href={links[title]}
        class="font-noto rounded-xl text-center text-2xl hover:bg-gray-100 hover:shadow-xs md:p-3"
      >
        {title}
      </PeridotHeaderLink>
    ))
  }
</div>
